package compare_test

import (
	"testing"
)

func TestCompare(t *testing.T) {
	/*
		TODO - initial implementation to just work on smaller lists. Will need to optimise this as the source data is huge (~40gb)
			matches := compare(hibp, wpm)
			for _, match := range matches {
				fmt.Printf("Matched: Item %s to username %s with SHA1 %s\n", match.name, match.username, match.sha1)
			}
	*/
	t.Fail()
}
